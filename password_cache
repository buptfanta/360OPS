1. password_cache:
运维操作在很多情况下需要访问不同的服务器，运维人员可能会频繁的输入自己的账号和密码。
是否可以通过一个简单方式只需要输入一遍密码，然后余下的操作都可以不再重复的输入密码。
密码应该存储在什么地方会比较隐蔽，让有这台机器sudo权限的其他人看不到。（可以不写代码，回答便可）

回答：
可以利用ssh无密码登录连接方式，使用ssh生成公钥，密钥，取一台机器将他的公钥拷贝到控制的剩下的集群中。密码就放在原处，将/etc/sudoers里ALL去掉。将其权限改
为不可读写。将密钥文件夹的权限设置为用户组和其他用户组不可读。
